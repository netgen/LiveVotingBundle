{% extends 'LiveVotingBundle::baseAdmin.html.twig' %}

{% block title %}Edit|Questions|LiveVoting{% endblock %}
{% block mainHeading %}Edit question{% endblock %}

{% block body -%}
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-lg-3">
        {{ form_start(edit_form) }}
        {{ form_end(edit_form) }}
      </div>
  </div>

  <div class="row secondary-footer">
    <div class="col-xs-12">
      <a href="{{ path('admin_question', {'event_id':entity.event.id}) }}"><button type="button" class="btn">Back</button></a>
    </div>
  </div>
{% endblock %}
