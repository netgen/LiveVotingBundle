{% extends 'LiveVotingBundle::base.html.twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('bundles/livevoting/css/loader.css') }}"  />
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-xs-12">
			<h1>Live Voting</h1>
		</div>
	</div>
	{% for event in events %}
	{% if event.event %}
	<div class="row slave-event">
		<div class="col-xs-12 col-sm-11 col-sm-offset-1">
			<div class="wrapper">
				<div class="pull-left">
					<img src="{{asset('bundles/livevoting/')}}{{ event.getImage ? event.getWebPath : 'img/man1.png'}}">
					<h2 class="text-left">{{ event.getName }}</h2>
				</div>
				<div class="pull-right">
					<span><a href="{{path( 'event' , {'event_id':event.getId})}}" class="btn-event">Vote</a></span
					><span>{% if event.getAllowViewingResults %}<a href="{{path('result', {'event_id':event.getId})}}" class="btn-event">Results</a>{% endif %}</span
					><span>{% if event.questions|length > 0 %}<a href="{{path('question', {'event_id':event.getId})}}" class="btn-event">{{ event.questions|length }} {{ event.questions|length > 1 ? 'questions' : 'question' }}</a>{% endif %}</span>
				</div>
			</div>
		</div>
	</div>
	{% else %}
	<div class="row master-event">
		<div class="col-xs-12">
			<div class="wrapper">
				<h2 class="text-left">{{ event.getName }}</h2>
                <h3>{{ event.begin.format("D, d M Y H:i:s O") }}-{{ event.end.format("D, d M Y H:i:s O") }}</h3>
			</div>
		</div>
	</div>
	{% endif %}
	{% endfor %}

	<div class="row secondary-footer">
		<div class="col-xs-12">
			{% if userPresentationsNum != 0 %}
			<a href="{{ path('user_presentations') }}"><button type="button" class="btn"><i class="fa fa-clipboard"></i>My presentations</button></a>
			{% endif %}
			{% if(app.request.cookies.has('userEditEnabled')) %}
			<a href="{{ path('user_edit') }}"><button type="button" class="btn"><i class="fa fa-pencil"></i>Edit user info</button></a>
			{% endif %}
		</div>
	</div>
{% endblock %}

{% block javascripts %}
	<script type="text/javascript" src="{{ asset('bundles/livevoting/js/handlebars-v1.3.0.js') }}"></script>
	<script type="text/javascript" src="{{ asset('bundles/livevoting/js/script.js') }}"></script>
	<script type="text/javascript" src="{{ asset('bundles/livevoting/js/icheck.js') }}"></script>
{% endblock %}
