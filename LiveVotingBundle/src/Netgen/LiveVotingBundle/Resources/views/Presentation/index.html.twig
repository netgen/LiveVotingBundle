{% extends 'LiveVotingBundle::base.html.twig' %}

{% block body -%}
    <h1>Presentation list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Surname</th>
                <th>Presentation</th>
                <th>Voting</th>
                <th>Edit</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity[0].presenterName }}</td>
                <td>{{ entity[0].presenterSurname }}</td>
                <td>{{ entity[0].presentationName }}</td>
                <td>
                    {{ form(entity[1]) }}
                </td>
                <td>

                    <a class='btn btn-primary' href="{{ path('admin_presentation_edit', { 'id': entity[0].id }) }}">Edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_presentation_new', {'event_id':event_id}) }}">
                Create a new entry
            </a>
        </li>

        <li>
            <a href="{{ path('admin_event') }}">
                Back to events
            </a>
        </li>
    </ul>
    {% endblock %}
